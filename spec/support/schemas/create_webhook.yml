type: object
properties:
  url:
    type: string
    description: "The URL to send webhook requests to. It should be a fully qualified URL (e.g., https://example.com/webhook)"
    example: "https://myapp.com"
required:
  - url